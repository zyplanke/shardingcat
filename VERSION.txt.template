#工程版名(等于Maven工程名)
ProjectName  @projectname@

#工程版本号(等于Maven工程配置的版本号)
ProjectVersion  @projectversion@

#通过Maven进行build时 日期时间戳
BuildTime  @gitbuildtime@

#通过Maven进行build时 Git的HEAD指针指向的CommitHash(通常为所在Git Branch分支的最新提交)，40位的全格式
BuildOfGitCommitHash  @gitcommithash@

#通过Maven进行build时 主机名
BuildHost  @gitbuildhost@

#通过Maven进行build时 Git用户名(不是操作系统的登录用户名)
BuildHostName  @gitbuildusername@

#通过Maven进行build时 所在的Git分支名
GitBranch  @gitbranch@

#Git仓库URL
GitRepositoryUrl  @gitrepourl@
